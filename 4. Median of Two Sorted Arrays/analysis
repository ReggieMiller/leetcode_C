1.利用二分查找法
2.获取数组中固定位置
3.尾递归
4.极限值考虑
5.极限问题：某一数组为空。
6.边界问题：获取n后，对于n+1的获取要比较两个数组相同位置位移为1的数字大小。
7.脏数据问题：run的值和submit不一致，原因是submit时，数组2存放的地址紧接数组1，当考虑问题6.边界问题时，无法判断位移为1的数字是否为本数组，引入真实长度概念解决该问题
8.算法复杂度O(log(m+n))
9.数组在函数结束后会自动销毁，所以无法作为函数结果返回，所以需要申请空间或者作为全局变量处理。
10.引入中间数机制处理，针对奇数和偶数进行不同处理函数 mid()
11.可优化部分：计算奇数偶数使用不同处理逻辑，位置计算细分处理pos+1，极限值处理情况（数组长度为0），参数是否可优化，结构优化等。
